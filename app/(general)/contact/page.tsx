import React from "react";
import type { Metadata } from "next";

export const metadata: Metadata = {
	title: "contactttr",
	description: "the cont pagge",
	keywords: ["abc", "def", "hd"],
};

export default function ContactPage() {
	return (
		<>
			<span>Contact</span>
		</>
	);
}
